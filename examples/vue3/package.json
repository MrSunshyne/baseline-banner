{
  "name": "baseline-banner-vue3-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Vue 3 example using @baseline-banner/vue with script setup",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "@baseline-banner/vue": "workspace:*",
    "@baseline-banner/styles": "workspace:*"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "~5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
